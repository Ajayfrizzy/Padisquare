import PageSkeleton from '@/components/Skeletons';

/**
 * Loading component for the vendor page.
 * Uses skeleton components to maintain layout consistency.
 */
export default function Loading() {
  return <PageSkeleton />;
}
